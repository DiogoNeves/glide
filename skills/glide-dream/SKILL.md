---
name: glide-dream
description: Consolidate changed Glide evidence into compact context and candidate procedural lessons without losing source history.
---

Read `Glide HQ/Memory Protocol.md` and the instance's unresolved intake failures and batch limits. Start with compact `glide_job_inputs(job_id="dream")` descriptors; page relevant details through `glide_job_input_page` and follow the protocol's checkpoint contract. Process changed inputs after a successful intake, not merely because its scheduled start time has passed.

Consolidate a bounded batch: reuse existing concepts, retain evidence and historical distinctions, refresh compact context, flag weakened assumptions, and propose useful procedural lessons from actual outcomes. Return permitted successful outputs and their checkpoint through `glide_finish_job`, following the protocol's separate-review rule where required and using the returned input boundary and checkpoint revision. Preserve an unprocessed backlog and never finish a failed batch. If nothing changed, stop quietly without appending a run record.

Follow the protocol's explicit manual/automatic or legacy-authorized knowledge policy; record pending review honestly and never use a checkpoint to bypass it. Archive registration alone does not require semantic promotion. Apply routine memory changes only within existing authority. Accepted conclusions affected by counterevidence become review candidates; do not silently replace them. A dream does not create commitments, close loops without evidence, alter goals or delete originals.

Version candidate learned overlays with their evidence, exact change and rollback version. Use the protocol's `glide_overlay_evaluate`/`glide_overlay_activate` tools for eligible changes; never ordinary proposals to the reserved overlay record. Automatic activation follows the explicit opt-in and regression/held-out requirements in the protocol; ordinary consolidation is not proof that an overlay works. Keep the user-facing result to material changes, required judgment or failures.

When conversation recovery is enabled, run `glide-conversation-learning` for pending/recent authorized history before deciding there is no work. Consolidate useful candidates and commit coverage with permitted outputs in this dream batch; retrieved candidates do not become active guidance.
